<div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
          <div class="card">
              <div class="card-header">Perfil de Usuario</div>
              <div class="card-body">
                {{#if message}}
                <div class="alert alert-success" role="alert">{{ message}}</div>
                {{/if}}

                <div class="card-body">
                    <p class="card-text">Nombre: <span id="first_name">{{user.first_name}}</span></p>
                    <p class="card-text">Apellido: <span id="last_name">{{user.last_name}}</span></p>
                    <p class="card-text">Email: <span id="email">{{user.email}}</span></p>
                    <p class="card-text">Edad: <span id="age">{{user.age}}</span></p>
                </div>

                <form id="productForm" action="/api/sessions/logout" method="POST">
                    <button type="submit" class="btn btn-danger">Cerrar Sesión</button>
                </form>

                <br>

              </div>
          </div>
      </div>
    </div>
</div>
  <br><br></br>